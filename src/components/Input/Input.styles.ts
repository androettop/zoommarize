import { styled } from "../../theme";

export const StyledInput = styled("input", {
    fontSize: "14px",
    lineHeight: "19px",
    fontWeight: 400,
    fontFamily: "$default",
    color: "$gray",
    backgroundColor: "$darker",
    border: 0,
    borderRadius: "8px",
    padding: "8px 12px",
});
